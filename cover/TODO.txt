TODO — ComfyUI Workflow Runner (PySide6 rewrite)
==================================================
Status as of this handoff. "DONE" = implemented and smoke-tested.
"PARTIAL" = working but simplified vs. what was asked.
"NOT STARTED" = not touched yet.


1. Drag images around inside a workflow
   PARTIAL — you can drag an image from one slot onto another slot to
   swap their contents. What's NOT done: true free-form pixel-level
   dragging of an image to an arbitrary position/canvas (there's no
   open canvas to place images anywhere — slots are still a fixed row).
   If you meant true freeform placement (like moving nodes on a canvas),
   that's a bigger feature and still needs to be scoped.

2. Change the order image slots appear in
   DONE — drag the "⋮⋮ drag to reorder" handle on a slot's header to
   move the whole slot (including its assigned image) to a new position.
   Order is saved per workflow tab.

3. Manually change the size of each image slot
   PARTIAL — slot WIDTH is resizable by dragging the splitter handle
   between slots, and this is saved per tab. Slot HEIGHT is not
   independently resizable yet (it's fixed/expands with the window).

4. General "Outputs" section listing ALL outputs, clearable via button
   DONE — Outputs tab shows every saved output image as a thumbnail grid,
   with "Clear All" (deletes files on disk, with a confirmation prompt),
   "Refresh," and "Open Folder."

5. Output section as its own tab, to the right of workflow tabs
   DONE — Outputs tab sits right after your workflow tabs and before
   Settings.

6. Settings as a tab, anchored at the far right of the tab bar
   DONE — Settings is always the last tab; dragging tabs to reorder is
   restricted so it (and Outputs, and "+") can't be dragged out of
   position.

7. Redesign the look of the app
   DONE (v1) — full dark theme via style.qss (colors, tab styling,
   buttons, inputs, scrollbars, etc.). This is a first pass — happy to
   iterate further on layout/spacing/branding once you've seen it.

8. Change the output path
   DONE — editable in the Settings tab, persisted to
   workflows_config.json.

9. Queue workflow runs and run them one after another
   DONE — "Add to Queue" on any workflow tab snapshots its current
   inputs; the Queue panel (dockable, visible from any tab) lists
   everything queued. "Run Queue" runs items strictly one at a time,
   waiting for each ComfyUI job to fully complete (success or error)
   before starting the next. NOT done: reordering queued items by drag,
   pausing/cancelling a run in progress, or a thumbnail preview per
   queue entry.

10. Full hotkey functionality, with an overview in Settings
    PARTIAL — a solid starter set of hotkeys is wired up (run, queue,
    tab navigation, open Outputs/Settings, refresh, etc.) and listed in
    a table on the Settings tab. NOT done: letting you REMAP/customize
    hotkeys yourself from the UI — right now they're fixed in code
    (see HOTKEYS list and _setup_hotkeys() in main.py if you want to
    hand-edit them meanwhile).

11. "...and some more"
    You mentioned there were additional things beyond the essentials —
    still need that list from you to scope and implement.


OTHER GAPS WORTH KNOWING ABOUT
-------------------------------
- No connection/health indicator for whether the ComfyUI server is
  actually reachable before you hit Run.
- No retry/backoff if the network drops mid-upload or mid-poll.
