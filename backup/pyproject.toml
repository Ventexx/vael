[project]
name = "backup-utility"
version = "1.0.0"
description = "Single-file 7-Zip-based backup utility"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]

# Python version
# The code uses `from __future__ import annotations`. Tested on 3.12 and 3.14.

# 7-Zip version
# Verified: 7-Zip 23.01 on Linux, and NanaZip 6.5 on Windows 11
# (NanaZip is a Windows Store app wrapping the 7-Zip core)
